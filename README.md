# Serve-D Syntax for [Nova][Nova]

More information about the plugin can be found in the
`Serve-D.novaextension` directory.

This includes the Tree-sitter syntax support for D.

## Building

You can build using just "make".
